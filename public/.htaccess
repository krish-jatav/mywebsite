# Enable rewrite engine
RewriteEngine On

# Serve existing files/folders if they exist
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# If not found → trigger 404.html
ErrorDocument 404 /404.html
RewriteRule .* - [R=404,L]